"""
periodic_table.json:
Special thanks to Pymatgen Development Team for
providing the file "periodic_table.json".
format: {symbol: {attr: val, attr: val}, symbol: {attr: val, attr: val}}
    symbol (str): the symbol of the element, e.g., 'Ac'
    attr (str): the attribute of the element, e.g., 'Atomic mass'
    val: the value of the attribute

incarTags.json:
Storing all the INCAR tags name and their wiki links.
format: [{'tag': tag, 'link', link}, {'tag': tag, 'link', link}]
    tag (str): the name of the tag
    link (str): the wiki link of the tag

"""
